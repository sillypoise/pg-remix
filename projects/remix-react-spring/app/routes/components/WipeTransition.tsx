import { useLocation } from "@remix-run/react";

export default function WipeTransition() {
    return (
        <div className="imposter inset-[0] transform-none bg-dark-gray-11 z-10 cover"></div>
    );
}
