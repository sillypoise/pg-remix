{
    "private": true,
    "sideEffects": false,
    "scripts": {
        "build": "remix build",
        "deploy": "fly deploy --remote-only",
        "dev": "remix dev",
        "start": "remix-serve build"
    },
    "dependencies": {
        "@remix-run/node": "^1.7.0",
        "@remix-run/serve": "^1.7.0"
    },
    "devDependencies": {},
    "engines": {
        "node": ">=14"
    }
}
